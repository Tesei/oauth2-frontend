@page
@using FunSun.Auth.Pages.AccessDenied
@inject IStringLocalizer<IndexModel> Localizer
@model IndexModel
@{
    ViewData["Title"] = Localizer["Title"];
}

<div>
    <h1 class="text-danger">@Localizer["Title"]</h1>
    <p class="text-danger">@Localizer["ErrorDetails"]</p>
</div>